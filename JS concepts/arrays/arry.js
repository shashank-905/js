// array=arry is the variable like structure that can hold more than one value

let fruits=["apple","mango","banana","guva"]
// index=fruits.indexOf("guva");
// fruits.pop();
// fruits.push("hii");
// index=fruits.length;
// console.log(index);

fruits.sort().reverse();
for(let fruit of fruits){

// for(let i=0;i<fruits.length;i++){
console.log(fruit);
}
// 
